<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext HTTP is needed for the ESP32 provisioning portal (192.168.4.1)
         and for the DuckDNS domain used to reach the controller on the LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
